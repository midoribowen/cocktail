import { capitalizeHelper } from 'cocktail/helpers/capitalize-helper';
import { module, test } from 'qunit';

module('Unit | Helper | capitalize helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = capitalizeHelper([42]);
  assert.ok(result);
});
